
#include "PaintJob.h"

void CPaintJob::AddDecal(const vector3d& center, const vector3d& normal, const vector3d& tangent, float width, float height, float depth)
{
	// do nothing
	//m_decals.push_back( Decal(center, normal, tangent, width, height, depth) );
}

